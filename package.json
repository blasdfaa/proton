{
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel src/index.html build",
    "build": "npm run clean && npm run build:parcel"
  },
  "devDependencies": {
    "@parcel/transformer-image": "2.0.0-rc.0",
    "@parcel/transformer-sass": "2.0.0-rc.0",
    "autoprefixer": "^10.3.7",
    "parcel": "^2.0.0-rc.0",
    "sass": "^1.42.1"
  },
  "browserslist": [
    "> 0.2%",
    "not dead"
  ],
  "svgSpriteOptions": {
    "include": [
      "src/**/*"
    ]
  }
}
